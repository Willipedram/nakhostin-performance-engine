# Components

See [Component Intelligence](component-intelligence.md) for definitions, stable signatures, bundle composition, custom components, and integration adapters.

Administrator-defined IDs, selectors, dependency identifiers, states, cache behavior, and invalidation dependencies are nonce/capability protected and strictly sanitized. Component JavaScript fields describe dependencies; they do not accept or execute arbitrary JavaScript source code.
